# Optional. Only needed in production if the frontend and backend are
# deployed to different origins (e.g. frontend on Vercel, backend on
# Render/Railway). Leave unset for local dev — vite.config.js already
# proxies /api to http://localhost:4000.
#
# VITE_API_URL=https://your-backend.example.com/api
